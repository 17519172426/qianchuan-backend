buildCommand: npm install && npm start
env:
  - key: NODE_ENV
    value: production
  - key: ACCESS_TOKEN
    sync: false
  - key: REFRESH_TOKEN
    sync: false
  - key: APP_ID
    sync: false
  - key: APP_SECRET
    sync: false

